<h2>Projects</h2>
<div class="card-container">
    {% for item in site.data.education %}
    {% endif %}
    <div class="card">
      <div class="info">
        <p class="Course">{{ item.course }}</p>
        <p class="institue">{{ item.institute }}</p>
        <p class="dates">{{ item.dates }}</p>
      </div>
    </div>
    {% endfor %}
  </div>
